from setuptools import setup

setup(
    name='sizetools',
    version='1.0.0',
    url='https://github.com/max0d41/sizetools',
    description='Functions to convert size values from and to a human readable format',
    packages=[
        'sizetools',
    ],
    zip_safe=False
)
